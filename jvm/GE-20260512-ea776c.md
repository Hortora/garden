---
id: GE-20260512-ea776c
garden: discovery
title: "Quarkus named persistence units silently skip schema generation — explicit config required per named PU"
type: gotcha
domain: jvm
stack: "Quarkus, Hibernate ORM, Flyway"
tags: [quarkus, hibernate, flyway, named-datasource, schema-generation, multi-tenancy]
score: 12
verified: true
staleness_threshold: 730
verified_on: "quarkus: 3.34.x"
submitted: 2026-05-12
author: mdp
constraints: "Applies when using multiple named Hibernate ORM persistence units alongside the default PU"
invalidation_triggers: "Revisit if Quarkus changes named PU config inheritance behaviour"
---

## Quarkus named persistence units silently skip schema generation — explicit config required per named PU

### Symptom

Named persistence unit tables are not created or migrated at startup. No error. The default persistence unit works correctly. Adding `quarkus.hibernate-orm.database.generation=drop-and-create` has no effect on the named PU.

### What was tried (didn't work)

- Setting `quarkus.hibernate-orm.database.generation` — applies to default PU only, no effect on named PUs
- Setting `quarkus.flyway.migrate-at-start=true` — applies to default datasource only

### Root cause

`quarkus.hibernate-orm.database.generation` and `quarkus.flyway.migrate-at-start` apply exclusively to the **default** persistence unit. Named persistence units require their own explicit configuration under the named prefix — they do not inherit from the default.

### Fix

For every named PU, replicate the schema generation and migration config under the named prefix:

```properties
# For a PU named "qhorus"
quarkus.hibernate-orm.qhorus.datasource=qhorus
quarkus.hibernate-orm.qhorus.database.generation=none
quarkus.flyway.qhorus.migrate-at-start=true
```

### Why this is non-obvious

The default PU config is global-looking — developers expect it to be a default that named PUs inherit. There is no warning when a named PU has no schema generation config; it simply starts with no schema and queries fail later with table-not-found errors that appear to be a data or migration problem rather than a config gap.

**See also:** [GE-20260521-2b82e7](GE-20260521-2b82e7.md) — Panache.withTransaction() uses default persistence unit silently

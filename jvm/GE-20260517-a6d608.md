---
id: GE-20260517-a6d608
garden: discovery
title: "DefaultBean @ApplicationScoped + MicroProfile Config @ConfigProperty enables zero-config SPI with deployment-level overrides"
type: technique
domain: jvm
stack: "Quarkus 3.32.2, MicroProfile Config, CDI"
tags: [quarkus, CDI, DefaultBean, ConfigProperty, SPI, technique]
score: 11
verified: true
staleness_threshold: 730
submitted: 2026-05-17
author: mdp
constraints: "Requires Quarkus CDI (ArC). DefaultBean displacement is CDI-spec behaviour — works in any CDI 4.0+ container but the @DefaultBean annotation is Quarkus-specific."
invalidation_triggers: "Revisit if Quarkus changes @DefaultBean semantics or if CDI 5.0 standardises a default-bean mechanism."
---

### What it is
Combine `@DefaultBean @ApplicationScoped` with `@ConfigProperty` to create an SPI implementation that:
1. Works out of the box with hardcoded fallback values (zero config required)
2. Is configurable via `application.properties` without writing code
3. Is fully overridable by deployers who provide their own `@ApplicationScoped` implementation without `@DefaultBean`

### Pattern

**Interface in `api/` (pure Java, no framework):**
```java
public interface DeviationResponsePolicy {
    DeviationResponseRequirements evaluate(DeviationContext context);
}
```

**Default implementation in `runtime/` with three tiers of control:**
```java
@ApplicationScoped
@DefaultBean  // displaced by any @ApplicationScoped impl without @DefaultBean
public class DefaultDeviationResponsePolicy implements DeviationResponsePolicy {

    // Tier 2: config-file customisation (no code required)
    @ConfigProperty(name = "casehub.clinical.deviation.minor.deadline", defaultValue = "PT168H")
    Duration minorDeadline;

    @ConfigProperty(name = "casehub.clinical.deviation.major.deadline", defaultValue = "PT72H")
    Duration majorDeadline;

    @Override
    public DeviationResponseRequirements evaluate(DeviationContext context) {
        // Tier 1: hardcoded defaults (zero config required — works as-is)
        return switch (context.severity()) {
            case MINOR -> new DeviationResponseRequirements(minorDeadline, EscalationRequirement.NONE);
            case MAJOR -> new DeviationResponseRequirements(majorDeadline, EscalationRequirement.SPONSOR_NOTIFICATION);
            case CRITICAL -> new DeviationResponseRequirements(criticalDeadline, EscalationRequirement.IRB_REVIEW);
        };
    }
}
```

**Deployer override (Tier 3: full code control):**
```java
@ApplicationScoped  // no @DefaultBean → displaces the default via CDI
public class MyOrganisationDeviationPolicy implements DeviationResponsePolicy {
    // Scope by trial, site, phase, sponsor — no changes to application code
    @Override
    public DeviationResponseRequirements evaluate(DeviationContext context) {
        if (context.phase() == TrialPhase.PHASE_I) {
            return new DeviationResponseRequirements(Duration.ofHours(12), EscalationRequirement.IRB_REVIEW);
        }
        // ...
    }
}
```

### Why this is non-obvious
Most developers either hardcode defaults or require explicit configuration. The three-tier pattern (hardcode → config property → full SPI override) is rarely seen together. `@DefaultBean` is a Quarkus-specific annotation not in CDI spec, making it easy to overlook. The layering lets a policy be "good enough to ship" immediately while remaining fully customisable without forking the application.

**See also:** [GE-20260513-4f26a7](../quarkus/GE-20260513-4f26a7.md) — simpler @DefaultBean layer displacement without config property | [GE-20260427-543663](../quarkus/GE-20260427-543663.md) — @Produces @DefaultBean for library-level overridable defaults

# v:24d896 model=gemma3:4b count=3
`Instant.toString()` and Jackson response differ on nanosecond precision
`REST Assured equalTo(Instant.toString())` fails intermittently
`Jackson InstantSerializer` format variance causes discrepancies

---
id: GE-20260713-905e2e
garden: discovery
title: "IntelliJ MCP ide_create_file writes to VFS only — file does not exist on disk for Maven"
type: gotcha
domain: tools
stack: "IntelliJ Index MCP Plugin, Maven"
tags: [intellij, mcp-server, tooling, maven]
score: 10
verified: true
staleness_threshold: 730
submitted: 2026-07-13
author: "mdp"
constraints: "Applies when using ide_create_file from the IntelliJ Index MCP plugin (mcp__intellij-index__) to create new source files that must be compiled by Maven"
invalidation_triggers: "Revisit if the IntelliJ Index MCP plugin adds disk-persistence for ide_create_file"
---

### Symptom
`ide_create_file` returns success. `ide_build_project` passes — the new class is fully
resolved, references compile, diagnostics are clean. But `mvn compile` fails with
`cannot find symbol` for the class that was just created.

### Context
When creating a new `.java` file via `mcp__intellij-index__ide_create_file`, the file
is written to IntelliJ's Virtual File System (VFS) and immediately indexed. All
IntelliJ-based operations (build, diagnostics, find references) see the file. But
the VFS write does not always flush to the physical filesystem — Maven, which reads
directly from disk, cannot see it.

### Root cause
`ide_create_file` operates through IntelliJ's `VirtualFileManager` which maintains an
in-memory file tree. The VFS-to-disk flush is asynchronous and may not complete before
Maven is invoked. `ide_sync_files` does not force a VFS→disk flush for new files — it
syncs disk→VFS (the opposite direction).

### Fix
Use the `Write` tool (Claude Code's native file writer) to create new source files
instead of `ide_create_file`. `Write` persists directly to disk. After writing, call
`ide_sync_files` to make IntelliJ's VFS aware of the new file.

```
# For NEW files: Write tool → disk, then sync to IntelliJ
Write(file_path="/path/to/NewClass.java", content="...")
ide_sync_files(paths=["path/to/NewClass.java"])

# For EXISTING files: IntelliJ MCP tools work correctly
ide_edit_member(...)   # reads and writes through VFS, which IS flushed for edits
ide_insert_member(...) # same — edits to existing files persist
```

### Why this is non-obvious
`ide_create_file` reports success and the file passes all IntelliJ verification.
The IntelliJ build (`ide_build_project`) compiles cleanly. The failure only surfaces
when an external build tool (Maven, Gradle CLI) is invoked — which may be many steps
later. The symptom (`cannot find symbol`) points at a missing import or typo, not a
missing file.

### Alternatives considered
- `ide_sync_files` after `ide_create_file` — does not help; sync is disk→VFS, not VFS→disk
- Waiting/retrying — unreliable; the flush timing is non-deterministic

## Variant — Multi-project workspace resolves relative paths against wrong project

### Symptom
`ide_create_file` with `project_path="/path/to/projectA"` and
`file="module/src/.../NewClass.java"` creates the file under a different project
(e.g. `/path/to/projectB/module/src/.../NewClass.java`) when both projects share
a module name like `memory-api`.

### Context
When multiple Maven projects are open in an IntelliJ workspace (via
`ide_open_workspace`), `ide_create_file` resolves relative file paths against the
workspace root project, not the project specified by `project_path`. If two projects
share a submodule name (e.g. both have a `memory-api` module), the file lands in the
wrong project entirely.

### Root cause
The workspace root project's content roots are searched first for relative path
resolution. When a matching module directory exists under the workspace root, it
wins — regardless of the `project_path` parameter.

### Fix
Same as the main entry: use the `Write` tool with **absolute** file paths. Never
rely on `ide_create_file` with relative paths in multi-project workspaces.

```
# WRONG — resolves against workspace root, not project_path
ide_create_file(project_path="/path/to/neocortex",
                file="memory-api/src/.../NewClass.java", ...)

# CORRECT — absolute path, always lands in the right place
Write(file_path="/path/to/neocortex/memory-api/src/.../NewClass.java", ...)
```

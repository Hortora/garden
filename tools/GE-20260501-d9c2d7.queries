# v:24d896 model=gemma3:4b count=3
`GH_TOKEN` returns `403` cross-repo `repository_dispatch`
`secrets.GITHUB_TOKEN` error "Resource not accessible by integration"
classic PAT scope for `repository_dispatch`
